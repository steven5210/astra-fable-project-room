Review the attached task-list filtering specification independently. Explain what you believe the feature does before evaluating it.

Identify unclear behavior, missing constraints, and acceptance criteria that cannot be verified. For each blocking finding, explain its consequence and the smallest change that resolves it. Keep optional enhancements outside the agreed scope.

Use `accept` only if the exact attached revision is ready for the next planning step with no blocking issue. Otherwise use `changes_required`. Copy the revision and SHA-256 from the room's packet into the required structured output. This request authorizes a read-only review only; do not write files, implement code, run builds/tests, or delegate work.
